class Solution {
    public boolean isSorted(ArrayList<Integer> arr) {
        for(int i=1;i<arr.size();i++){
            if(arr.get(i)>=arr.get(i-1)){

            }else{
                return false;
            }
        }
        return true;
    }
}
